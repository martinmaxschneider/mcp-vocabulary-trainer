# Optional: run the Next.js app in Docker with a persisted SQLite file.
# Local development usually needs no Docker — just `npm run dev`.

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sprachen-app
    ports:
      - "4810:4810"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:/app/data/sprachen.db
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - PORT=4810
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4810/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
